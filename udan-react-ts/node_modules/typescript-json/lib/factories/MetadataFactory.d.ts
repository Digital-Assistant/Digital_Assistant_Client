import ts from "typescript";
import { Metadata } from "../metadata/Metadata";
import { MetadataCollection } from "./MetadataCollection";
export declare namespace MetadataFactory {
    interface IOptions {
        resolve: boolean;
        constant: boolean;
    }
    function generate(checker: ts.TypeChecker, collection: MetadataCollection, type: ts.Type | null, options: IOptions): Metadata;
}
