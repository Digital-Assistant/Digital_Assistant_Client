import ts from "typescript";
import { IProject } from "../IProject";
export declare namespace IsTransformer {
    function transform(equals: boolean): (project: IProject, modulo: ts.LeftHandSideExpression, expression: ts.CallExpression) => ts.Expression;
}
