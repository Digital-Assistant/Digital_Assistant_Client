import { Metadata } from "../metadata/Metadata";
import { IJsonApplication } from "../schemas/IJsonApplication";
export declare namespace ApplicationProgrammer {
    const AJV_PREFIX = "components#/schemas";
    const SWAGGER_PREFIX = "#/components/schemas";
    interface IOptions {
        purpose: "swagger" | "ajv";
        prefix: string;
    }
    function generate(metadatas: Array<Metadata>, options?: Partial<IOptions>): IJsonApplication;
}
