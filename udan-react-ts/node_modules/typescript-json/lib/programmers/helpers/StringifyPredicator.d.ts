import { Metadata } from "../../metadata/Metadata";
export declare namespace StringifyPredicator {
    function require_escape(value: string): boolean;
    function undefindable(meta: Metadata): boolean;
}
